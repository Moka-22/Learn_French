import 'package:flutter/material.dart';

import 'colors.dart';

const emailIcon = Icon(Icons.email_outlined);
const passwordIcon = Icon(Icons.lock);
const passwordIconEyeOn = Icon(Icons.visibility_rounded);
const passwordIconEyeOff = Icon(Icons.visibility_off_rounded);
final homeIcon = Icon(Icons.home, color: primaryColor);
final favoriteIcon = Icon(
  Icons.favorite,
  color: primaryColor,
);
final favoriteIconFilled = Icon(
  Icons.favorite_border_outlined,
  color: primaryColor,
);
final aboutUsIcon = Icon(
  Icons.person,
  color: primaryColor,
);
final settingIcon = Icon(
  Icons.settings,
  color: primaryColor,
);
final logOutIcon = Icon(
  Icons.logout_outlined,
  color: primaryColor,
);
final editProfileIcon = Icon(
  Icons.edit,
  color: primaryColor,
);
final contactUsIcon = Icon(
  Icons.contact_support,
  color: primaryColor,
);
final notificationIcon = Icon(Icons.notifications);
